[package]
name = "aoc2025_12"
version = "0.1.0"
edition = "2024"

[dependencies]
aoc_utils = { version = "*", path = "../../aoc_utils" }

[dev-dependencies]
# Used to make writing tests for the puzzle shapes a bit easier.
pretty_assertions = "1.4"
indoc = "2.0"
